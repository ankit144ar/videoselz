const productRepository = require('./productRepository');
const videoRepository = require('./videoRepository');
const engagementEventRepository = require('./engagementEventRepository');

module.exports = {
  productRepository,
  videoRepository,
  engagementEventRepository
};